### Why do I NOT get "Hey Sage" returned in the function below:

    function myGreeting (person) {
        if (person == { name: 'Sage' }) {
            return 'Hey Sage'
        } else {
            return 'Hey Alan'
        }
    }

    myGreeting({ name: 'Sage' })


### What would you change to address this issue?